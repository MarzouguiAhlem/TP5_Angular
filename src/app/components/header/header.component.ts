// Import necessary modules and components from Angular core
import { Component } from '@angular/core';

// Component decorator to define metadata for the component
@Component({
  selector: 'app-header', // Selector for using the component in templates
  templateUrl: './header.component.html', // Template file for the component
  styleUrls: ['./header.component.css'], // Stylesheet file for the component
})
export class HeaderComponent {
}
